# Cluster-runtime requirements — minimal subset actually needed at runtime.
# neo4j, tree-sitter*, python-dotenv are NOT listed here because their imports
# in pipeline.py / harness.py / parsers/ have been made lazy (TYPE_CHECKING
# guards and deferred imports inside functions that are never called on cluster).
# torch/numpy/scipy already in pytorch_pbs base image.
# bert-score is excluded on purpose: pass --no-bertscore to
# scripts.rejudge_quality_and_overlap (BLEU-4, ROUGE-L and METEOR still run;
# bertscore_f1 is left empty). roberta-large could not be fetched from a
# network-isolated compute node anyway.

pydantic==2.10.4
gitpython==3.1.44
ollama==0.4.7
httpx==0.28.1
sqlalchemy==2.0.36
pandas==2.2.3
nltk==3.10.3
rouge-score==0.1.2
openpyxl==3.1.2
scikit-learn
